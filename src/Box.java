/**
 * The <code>enum</code> lists all the possible configurations of a box on the
 * Tic-Tac-Toe board. In this case, we use it to represent the possible
 * configurations of a box on the game board, which can be either X, O, EMPTY.
 * 
 * @author Yifan Wang
 */
public enum Box {
	X, O, EMPTY;
}
